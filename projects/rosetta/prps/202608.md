優化 Admin TranslationSource

- [x] 編輯後，導向show而非 index 
      c8fe14d70be85673f99e697171273a461651dded
- [ ] index 頁可以用 ~~strategy (ex: kotoba)~~/ pattern(ex: n1) 篩選
- [ ] index 頁要顯示 kotoba remote model name

直接用輸入文字的方式 filter，不需用下拉選單 + 按 submit
- 甚至可以搜 `*.name` 找到所有 name columns(TS)
支援瀏覽  namespace.* 合併顯示的模式